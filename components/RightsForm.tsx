'use client';import{useState}from'react';export function RightsForm(){const[message,setMessage]=useState<string>();const[form,setForm]=useState({requesterName:'',email:'',type:'ACCESS'});async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/rights',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();setMessage(r.ok?`Request ${d.data.id} received. Target response: ${new Date(d.data.dueAt).toLocaleDateString()}.`:d.error)}return <form className="form" onSubmit={submit}><input placeholder="Full name" required value={form.requesterName} onChange={e=>setForm({...form,requesterName:e.target.value})}/><input type="email" placeholder="Email address" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}>{['ACCESS','CORRECTION','ERASURE','CONSENT_WITHDRAWAL','GRIEVANCE'].map(x=><option key={x}>{x}</option>)}</select><button>Submit request</button>{message&&<div className="callout">{message}</div>}</form>}
