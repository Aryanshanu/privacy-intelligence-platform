'use client';import{useState}from'react';export function Copilot(){const[q,setQ]=useState('Review this privacy policy for DPDP compliance.');const[r,setR]=useState<any>();async function ask(){const x=await fetch('/api/copilot',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q})});setR((await x.json()).data)}return <div className="form"><textarea rows={6} value={q} onChange={e=>setQ(e.target.value)}/><button onClick={ask}>Analyze with Privacy Copilot</button>{r&&<div className="card"><b>Answer</b><p>{r.answer}</p><div className="callout"><b>Citation:</b> {r.citations[0].source}, {r.citations[0].section}</div>{r.gaps.map((g:any)=><div className="finding" key={g.title}><span><span className="badge high">{g.severity}</span> <b>{g.title}</b></span><span className="muted">{g.action}</span></div>)}</div>}</div>}
